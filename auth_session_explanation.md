# شرح نظام المصادقة وإدارة الجلسات في تطبيق مديري

هذا الملف يشرح بالتفصيل كيف يعمل كود المصادقة (`AuthService`)، ولماذا يظل المستخدم مسجلاً للدخول لفترات طويلة.

---

## 1. تحليل الكود (AuthService)
الأسطر التي طلبت شرحها (من 30 إلى 60 في `auth_service.dart`) هي المسؤولة عن **إنشاء حساب جديد** وحفظ بياناته. إليك ما يحدث خطوة بخطوة:

```dart
// 1. التحقق من الإنترنت
if (!_connectivity.isConnected) {
  return AuthResult.error('لا يوجد اتصال بالإنترنت...');
}

try {
  // 2. محاولة إنشاء الحساب في Supabase
  final response = await _supabase.signUp(email, password);
  
  if (response.user != null) {
    // 3. حفظ "الجلسة" في قاعدة البيانات المحلية (SQLite)
    // هذا يستخدم لعرض بيانات المستخدم داخل التطبيق بسرعة
    await _localStorage.saveUserSession(
      response.user!.id,
      email,
      displayName,
    );
    
    // 4. حفظ بيانات الدخول في المخزن الآمن (Secure Storage)
    // هذا يسمح لنا بتسجيل الدخول تلقائياً لاحقاً حتى لو لم يكن هناك إنترنت
    await _secureStorage.write(key: 'email', value: email);
    await _secureStorage.write(key: 'password', value: password);
    
    return AuthResult.success(response.user!);
  }
```

### لماذا نحفظ البيانات مرتين؟
*   **في `_localStorage` (SQLite):** لحفظ بيانات ملف المستخدم (الاسم، الإيميل) وربطها بالمصاريف والميزانية.
*   **في `_secureStorage` (Encrypted):** لحفظ "المفتاح" (كلمة المرور أو التوكن) بشكل مشفر وآمن جداً، لنستخدمه في الدخول التلقائي عند فتح التطبيق.

---

## 2. كيف تعمل الجلسة طويلة المدى؟ (Session Persistence)

عندما يسجل المستخدم دخوله (سواء بجوجل أو بالإيميل)، تقوم **Supabase** بإصدار نوعين من الرموز (Tokens):

### أ. رمز الوصول (Access Token) 🛡️
*   **وظيفته:** هو "الهوية" التي يرسلها التطبيق مع كل طلب (مثلاً: جلب المصاريف) ليثبت للسيرفر أنك "محمد".
*   **مدته:** قصيرة جداً (**ساعة واحدة فقط**).
*   **السبب:** للأمان. لو سرق أحدهم هذا الرمز، لن يستطيع استخدامه إلا لفترة قصيرة جداً.

### ب. رمز التحديث (Refresh Token) 🔄
*   **وظيفته:** هو "المفتاح الرئيسي" السري المحفوظ في جهازك.
*   **مدته:** **غير محددة (طويلة جداً)**. يستمر لسنوات.
*   **كيف يعمل:**
    1.  عندما تنتهي الساعة وينتهي "رمز الوصول".
    2.  يقوم التطبيق (مكتبة Supabase في الخلفية) بإرسال "رمز التحديث" للسيرفر.
    3.  السيرفر يتحقق منه، وإذا كان صالحاً، يعطي التطبيق "رمز وصول" جديد لمدة ساعة أخرى.
    4.  تتكرر هذه العملية للأبد.

### النتيجة بالنسبة للمستخدم:
المستخدم **لا يشعر بهذا أبداً**. يفتح التطبيق فيجد نفسه مسجلاً للدخول دائماً، كأن الجلسة لا تنتهي أبداً.

---

## 3. متى تنتهي الجلسة؟
تنتهي الحالة الوحيدة في حالتين:
1.  **تسجيل الخروج اليدوي:** عندما يضغط المستخدم زر "خروج"، يقوم التطبيق بحذف "رمز التحديث" من الجهاز، ويطلب من السيرفر إلغاءه.
2.  **تغيير كلمة المرور:** إذا قام المستخدم بتغيير كلمة مروره من جهاز آخر، فإن السيرفر يلغي كل رموز التحديث القديمة.

---

## الخلاصة
نظامك الحالي يجمع بين الأمان (Access Token قصير) والراحة (Refresh Token طويل)، وهذا هو المعيار العالمي (Standard) في تطبيقات مثل Gmail و Facebook وغيرها.
