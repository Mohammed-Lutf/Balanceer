# خطة تطوير وتحليل تطبيق "رصيد" (Balanceer)

هذا المستند يحتوي على تحليل شامل للكود الحالي، نقاط القوة والضعف، وخارطة طريق تفصيلية لتطوير التطبيق تقنياً ووظيفياً.

---

## 1. تحليل الكود الحالي (Code Audit)

### نقاط القوة (Strengths)
1.  **Offline-First:** الاعتماد على `sqflite` للتزين المحلي و `sync_service.dart` للمزامنة يضمن عمل التطبيق بكفاءة 100% بدون إنترنت.
2.  **الهيكلة (Folder Structure):** تقسيم المجلدات إلى `screens`, `services`, `models` واضح ومنطقي للمشاريع المتوسطة.
3.  **التصميم (UI/UX):** استخدام `flutter_animate`, `iconsax`, والتدرجات اللونية يعطي انطباعاً عصرياً. التصميم الجديد لـ `FlashyFAB` ممتاز.
4.  **الخدمات (Services):** فصل منطق الاتصال بالسيرفر وقاعدة البيانات في خدمات مستقلة (`SupabaseService`, `LocalStorageService`) هو ممارسة جيدة.

### نقاط الضعف وفرص التحسين (Weaknesses & Areas for Improvement)
1.  **إدارة الحالة (State Management):**
    *   **الحالي:** يعتمد التطبيق بشكل كبير على `setState` داخل الـ `Widgets` الكبيرة (مثل `HomeScreen` و `BudgetScreen`)، مع استخدام بسيط لـ `Provider` لحقن الخدمات.
    *   **المشكلة:** صعوبة صيانة الكود، تداخل منطق العرض مع منطق العمليات، وصعوبة اختبار الوحدات (Unit Testing).
    *   **الحل:** الانتقال الكامل إلى **Riverpod** لفصل الحالة عن الواجهة.

2.  **تضخم الملفات (Monolithic Widgets):**
    *   بعض الشاشات (مثل `home_screen.dart`, `budget_screen.dart`) تتجاوز 700-1000 سطر. هذا يجعل قراءة الكود وتعديله صعباً.
    *   يجب تقسيم الشاشات إلى "مكونات صغيرة" (Small Reusable Widgets) في مجلد `widgets/`.

3.  **العولمة (Localization):**
    *   النصوص العربية "مكتوبة يدوياً" داخل الكود (`Hardcoded Strings`).
    *   سيصعب هذا دعم لغات أخرى مستقبلاً أو حتى تعديل النصوص العربية مركزياً. يجب استخدام ملفات `l10n` و `arb`.

4.  **العمليات المتكررة (Recurring Transactions):**
    *   لا يوجد نظام آلي لإدارة الاشتراكات الشهرية أو الرواتب الثابتة، مما يضطر المستخدم لإدخالها يدوياً كل شهر.

5.  **المعمارية (Architecture):**
    *   المنطق موزع بين الـ UI والـ Services. يفضل تطبيق **Clean Architecture** (Data, Domain, Presentation) لفصل المسؤوليات بشكل تام.

---

## 2. خارطة طريق التطوير (Development Roadmap)

تم تقسيم الخطة إلى 4 مراحل رئيسية، مرتبة حسب الأولوية والقيمة المضافة.

### المرحلة الأولى: أساس تقني متين (Technical Foundation)
**الهدف:** تحسين جودة الكود، تسهيل الصيانة، وتحسين الأداء.

- [ ] **Riverpod Refactoring:**
    - استبدال `Provider` و `setState` بـ `Riverpod`.
    - إنشاء `Providers` لكل ميزة (مثلاً `expensesProvider`, `authProvider`).
    - جعل الـ UI تفاعلياً (Reactive) بالكامل.
- [ ] **Componentization (التقسيم):**
    - تفكيك `HomeScreen` إلى: `HeaderWidget`, `BalanceCard`, `TransactionsList`.
    - تفكيك `BudgetScreen` إلى: `BudgetSummary`, `CategoryBudgetList`.
- [ ] **Dependency Injection:**
    - تحسين طريقة استدعاء السيرفسات باستخدام Riverpod بدلاً من تمريرها يدوياً في الكونستركتور.

### المرحلة الثانية: ميزات أساسية مفقودة (Missing Core Features)
**الهدف:** زيادة فائدة التطبيق للمستخدم اليومي.

- [ ] **المعاملات المتكررة (Recurring Transactions):**
    - إضافة خيار "تكرار" عند إضافة نفقة (يومياً، أسبوعياً، شهرياً).
    - تشغيل عملية في الخلفية لإضافة هذه النفقات تلقائياً.
- [ ] **البحث والفلترة المتقدمة:**
    - إضافة شريط بحث في صفحة "كل النفقات".
    - فلترة حسب: التاريخ (نطاق معين)، الفئة، المبلغ، والملاحظات.
- [ ] **إدارة الأهداف (Savings Goals):**
    - قسم جديد "أهداف الادخار" (مثلاً: شراء لابتوب).
    - اقتطاع جزء من الميزانية للأهداف.

### المرحلة الثالثة: الذكاء وتحسين التجربة (Smart Features & UX)
**الهدف:** جعل التطبيق "ذكياً" ومساعداً شخصياً.

- [ ] **تحليل الذكاء الاصطناعي (AI Analysis):**
    - تحليل نمط الصرف وتقديم نصائح (مثلاً: "صرفك على القهوة زاد بنسبة 20% هذا الشهر").
    - توقع المصاريف للشهر القادم.
- [ ] **Categorization Automation:**
    - اقتراح الفئة تلقائياً بناءً على اسم النفقة (مثلاً كتابة "برجر" تختار فئة "طعام").
- [ ] **ويدجت الشاشة الرئيسية (Home Widget):**
    - تطوير الـ Widget الحالي ليعرض "الرصيد المتبقي" وإضافة زر "إضافة سريع".

### المرحلة الرابعة: التوسع والاحترافية (Expansion)
**الهدف:** تجهيز التطبيق للنشر والمنافسة.

- [ ] **Localization (التعريب الاحترافي):**
    - نقل جميع النصوص إلى ملفات `arb`.
    - دعم اللغة الإنجليزية.
- [ ] **Testing:**
    - كتابة Unit Tests للعمليات الحسابية والمزامنة.
    - كتابة Widget Tests للعناصر الأساسية.
- [ ] **الأمان (Security Audit):**
    - مراجعة سياسات RLS في Supabase.
    - تشفير البيانات الحساسة محلياً بشكل أقوى.

---

## 3. مقترحات تصميمية (Design Suggestions)

1.  **الثيمات (Themes):** إضافة خيار لتخصيص لون التطبيق الرئيسي (ليس فقط الوضع الليلي، بل اختيار لون "أزرق"، "أحمر"، "أرجواني" كثيم كامل).
2.  **Gamification (التلعيب):** منح المستخدم "أوسمة" عند الالتزام بالميزانية 3 أشهر متتالية، لتشجيع الشباب.

## الخلاصة
التطبيق في حالة جيدة جداً كمنتج (MVP)، ولكن للانتقال للمستوى التالي، **يوصى بشدة بالبدء في "المرحلة الأولى" (Riverpod Refactoring)** قبل إضافة ميزات جديدة، لأن الكود الحالي سيزداد تعقيداً مع كل ميزة جديدة بدون معمارية قوية.
