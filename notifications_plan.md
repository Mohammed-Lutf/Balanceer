# خطة تطوير نظام الإشعارات (Notification System Plan)

يهدف نظام الإشعارات في تطبيق **Balanceer** إلى زيادة تفاعل المستخدم وضمان دقة البيانات من خلال تذكيرات ذكية وتنبيهات استباقية.

---

## 1. أنواع الإشعارات المقترحة (Notification Types)

### أ. إشعارات التذكير اليومي (Daily Reminders) - *محلي*
- **الهدف**: تذكير المستخدم بتسجيل مصاريف اليوم في وقت محدد (مثلاً 9 مساءً).
- **الفائدة**: ضمان عدم نسيان تسجيل أي نفقة.

### ب. تنبيهات الميزانية (Budget Alerts) - *محلي*
- **الهدف**: تنبيه المستخدم عند استهلاك 80% أو 100% من الميزانية المخصصة لفئة معينة.
- **الفائدة**: المساعدة في التحكم في الصرف قبل فوات الأوان.

### ج. ملخصات شهرية (Monthly Summaries) - *محلي/سحابي*
- **الهدف**: تقديم ملخص سريع لأداء الشهر المنصرم (إجمالي الصرف، الفئة الأكثر استهلاكاً).
- **الفائدة**: تشجيع المستخدم على مراجعة تقاريره.

### د. إشعارات المزامنة والتحديثات (Sync & Updates) - *سحابي*
- **الهدف**: إرسال عروض أو تحديثات جديدة للتطبيق أو تنبيهات أمنية.

---

## 2. المسار التقني (Technical Stack)

1.  **الإشعارات المحلية**: استخدام مكتبة `flutter_local_notifications`.
    - تدعم الجدولة (Scheduling).
    - لا تحتاج إلى إنترنت.
    - مثالية للتذكيرات اليومية وتنبيهات الميزانية.
2.  **إدارة الوقت**: استخدام `timezone` لضمان وصول الإشعارات في الوقت الصحيح حسب موقع المستخدم.
3.  **الإشعارات السحابية (Push Notifications)**:
    - **الخيار الأول**: Firebase Cloud Messaging (FCM).
    - **الخيار الثاني**: Supabase Edge Functions + OneSignal/FCM (نظراً لاستخدام Supabase في المشروع).

---

## 3. مراحل التنفيذ (Implementation Phases)

### المرحلة الأولى: التأسيس (Foundations)
- إعداد المتطلبات لكل من (Android & iOS).
- إنشاء `NotificationService` للتعامل مع تهيئة المكتبة وطلب الصلاحيات.
- تصميم الأيقونات الخاصة بالإشعارات.

### المرحلة الثانية: الإشعارات الذكية (Smart Local Notifications)
- جدولة تذكير يومي مرن يمكن للمستخدم تفعيله أو إلغاؤه من الإعدادات.
- إضافة منطق فحص الميزانية: في كل مرة يتم فيها إضافة نفقة، يتم التحقق برمجياً من النسبة المتبقية وإطلاق إشعار فوراً إذا تجاوزت الحد المسموح.

### المرحلة الثالثة: التخصيص (Customization)
- إضافة شاشة "إعدادات الإشعارات" للسماح للمستخدم بتحديد:
  - وقت التذكير اليومي.
  - أنواع التنبيهات التي يرغب في استلامها.
  - نغمة الإشعار.

---

## 4. مثال لمنطق عمل تنبيه الميزانية (Pseudocode)

```dart
void checkBudgetThreshold(double currentSpent, double totalBudget) {
  double ratio = currentSpent / totalBudget;
  if (ratio >= 0.8 && ratio < 1.0) {
    showNotification("تنبيه الميزانية", "لقد استهلكت 80% من ميزانية فئة الطعام!");
  } else if (ratio >= 1.0) {
    showNotification("تنبيه هام", "لقد تجاوزت الميزانية المخصصة لهذه الفئة!");
  }
}
```

---

## 5. التوصية (Recommendation)
ننصح بالبدء بـ **الإشعارات المحلية** أولاً (المرحلة الأولى والثانية) لأنها تناسب طبيعة التطبيق (Offline-First) ولا تتطلب تكاليف إضافية أو إعدادات معقدة في السيرفر.
